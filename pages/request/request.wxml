<!--pages/request/request.wxml-->

<view wx:for="{{fruitList}}" style="margin-top:30px;">
  <view>{{item.name}}</view>
  <view><image src="{{item.url}}"></image></view>
  <view>{{item.price}}元</view>
</view>
